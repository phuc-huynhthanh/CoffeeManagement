<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <link rel="icon" href="/assets/coffee.png" type="image/png">
  <title>Brother's Hai Coffee</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">

  <!-- Header -->
  <header class="bg-gradient-to-r from-blue-600 to-indigo-600 text-white px-8 py-4 flex justify-between items-center shadow-md">
    <h1 class="text-2xl font-bold tracking-wide">☕Brother's Hai Coffee</h1>
    <div class="flex gap-3 items-center">
      <input id="search" type="text" placeholder="Tìm kiếm sản phẩm..."
             class="px-4 py-2 rounded-lg text-gray-800 w-64 focus:outline-none focus:ring-2 focus:ring-yellow-400">
      <select id="filter" class="px-3 py-2 rounded-lg text-gray-800">
        <option value="">Tất cả loại</option>
        <% loai.forEach(l => { %>
          <option value="<%= l %>"><%= l %></option>
        <% }) %>
      </select>
      <div class="flex items-center gap-2 bg-white text-gray-800 px-3 py-1 rounded-full shadow-md">
        <p id="tenNhanVien" class="font-semibold text-sm"></p>
        <button id="logoutBtn" class="ml-2 text-red-500 hover:text-red-700 font-semibold text-sm">Đăng xuất</button>
      </div>
    </div>
  </header>

  <!-- Nội dung -->
  <div class="flex flex-1 overflow-hidden">
    <!-- Danh sách sản phẩm -->
    <main id="product-list" class="flex-1 grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 xl:grid-cols-5 gap-6 p-6 overflow-y-auto">
      <% sanPham.forEach(p => { %>
        <div 
          class="product bg-white rounded-2xl shadow hover:shadow-xl hover:scale-105 transform transition cursor-pointer flex flex-col items-center text-center p-4"
          data-id="<%= p.san_pham_id %>"
          data-ten="<%= p.ten_san_pham %>"
          data-gia="<%= p.gia_co_ban %>"
          data-loai="<%= p.ten_loai %>"
        >
          <img src="<%= p.hinh_anh %>" alt="<%= p.ten_san_pham %>" 
               class="w-28 h-28 object-cover rounded-xl mb-3">
          <h3 class="text-lg font-semibold text-gray-800"><%= p.ten_san_pham %></h3>
          <p class="text-gray-500 text-sm mb-1"><%= p.ten_loai %></p>
          <p class="text-blue-600 font-bold"><%= Number(p.gia_co_ban).toLocaleString() %> đ</p>
        </div>
      <% }) %>
    </main>

    <!-- Hóa đơn + Nút -->
    <aside class="w-full mt-2 sm:w-96 bg-white shadow-xl border-l rounded-tl-2xl flex flex-col">
      <div class="p-4 bg-blue-600 text-white font-semibold text-lg rounded-tl-2xl">
        🧾 Hóa đơn
      </div>
      <div id="bill" class="flex-1 p-4 overflow-y-auto">
        <p id="empty" class="text-gray-400 text-center mt-10">Chưa có sản phẩm nào</p>
      </div>
      <div class="border-t p-4 bg-gray-50 flex flex-col gap-2">
        <div class="flex justify-between mb-2 text-lg">
          <span>Tổng tiền:</span>
          <span id="total" class="font-bold text-blue-600">0 đ</span>
        </div>
        <button id="chooseTableBtn" class="w-full bg-yellow-500 hover:bg-yellow-600 text-white py-3 rounded-xl font-semibold transition">
          🍽 Chọn bàn
        </button>
        <button id="completeBtn" class="w-full bg-blue-500 hover:bg-blue-600 text-white py-3 rounded-xl font-semibold transition hidden">
          ✅ Hoàn tất
        </button>
        <button id="viewTablesBtn" class="w-full bg-green-500 hover:bg-green-600 text-white py-3 rounded-xl font-semibold transition">
    👀 Xem danh sách bàn
  </button>
      </div>
      <!-- Hóa đơn theo bàn -->
      <div id="billContainer" class="p-4"></div>
    </aside>
  </div>

  <!-- Modal danh sách bàn -->
  <div id="banContainer" class="hidden fixed inset-0 flex items-center justify-center z-50 bg-black bg-opacity-40">
    <div class="bg-white rounded-2xl shadow-xl p-6 w-96 max-h-[80vh] overflow-y-auto">
      <h2 class="text-lg font-semibold mb-4 text-center">Chọn bàn</h2>
      <div id="banList" class="grid grid-cols-3 gap-4"></div>
      <button onclick="closeBanModal()" class="mt-4 w-full bg-gray-200 py-2 rounded-lg hover:bg-gray-300">Đóng</button>
    </div>
  </div>

  <!-- Modal xem danh sách bàn + món -->
<div id="viewBanContainer" class="hidden fixed inset-0 flex items-center justify-center z-50 bg-black bg-opacity-40">
  <div class="bg-white rounded-2xl shadow-xl p-6 w-96 max-h-[80vh] overflow-y-auto">
    <h2 class="text-lg font-semibold mb-4 text-center">Danh sách bàn</h2>
    <div id="viewBanList" class="flex flex-col gap-4"></div>
    <button onclick="closeViewBanModal()" class="mt-4 w-full bg-gray-200 py-2 rounded-lg hover:bg-gray-300">Đóng</button>
  </div>
</div>

  <script>
document.addEventListener('DOMContentLoaded', () => {
  const bill = [];
  const billContainer = document.getElementById('bill');
  const totalEl = document.getElementById('total');
  const emptyEl = document.getElementById('empty');

  let kichCoList = [];
  fetch('/kichco/laytatca').then(r=>r.json()).then(data=>kichCoList=data).catch(console.error);
  let toppingList = [];
  fetch('/topping/laytatca').then(r=>r.json()).then(data=>toppingList=data).catch(console.error);

  // --- Thêm sản phẩm vào bill
  document.querySelectorAll('.product').forEach(el=>{
    el.addEventListener('click', ()=>{
      const sp = {
        san_pham_id: +el.dataset.id,
        ten_san_pham: el.dataset.ten,
        gia_co_ban: +el.dataset.gia,
        ten_loai: el.dataset.loai,
        kich_co: null,
        kich_co_id: null,
        gia_them: 0,
        toppings: [],
        tong_topping: 0
      };
      addToBill(sp);
    });
  });

  function addToBill(sp){
    const existing = bill.find(i=>i.san_pham_id===sp.san_pham_id && !i.kich_co);
    if(existing) existing.so_luong++;
    else bill.push({...sp, so_luong:1});
    renderBill();
  }

  function renderBill(){
    billContainer.innerHTML='';
    if(!bill.length) emptyEl.style.display='block';
    else {
      emptyEl.style.display='none';
      bill.forEach((item,i)=>{
        const div = document.createElement('div');
        div.className='flex justify-between items-center bg-gray-50 rounded-lg p-3 mb-2 shadow-sm cursor-pointer hover:bg-blue-50 transition';
        div.innerHTML=`
          <div class="flex-1" onclick="chonKichCoVaTopping(${i})">
            <p class="font-semibold text-gray-800">${item.ten_san_pham}</p>
            <p class="text-sm text-gray-500">
              ${item.kich_co || '(Chưa chọn size)'} 
              ${item.toppings.length ? ' + ' + item.toppings.map(t=>t.ten_topping).join(', ') : ''}
              — ${(Number(item.gia_co_ban)+Number(item.gia_them||0)+Number(item.tong_topping||0)).toLocaleString()} đ
            </p>
          </div>
          <div class="flex items-center gap-2">
            <button class="px-2 py-1 bg-gray-200 rounded hover:bg-gray-300 text-gray-700" onclick="changeQty(${i},-1)">-</button>
            <span class="w-6 text-center">${item.so_luong}</span>
            <button class="px-2 py-1 bg-gray-200 rounded hover:bg-gray-300 text-gray-700" onclick="changeQty(${i},1)">+</button>
            <button class="ml-2 text-red-500 hover:text-red-700" onclick="removeItem(${i})">🗑️</button>
          </div>
        `;
        billContainer.appendChild(div);
      });
    }
    const total = bill.reduce((sum,item)=>sum+(Number(item.gia_co_ban)+Number(item.gia_them||0)+Number(item.tong_topping||0))*item.so_luong,0);
    totalEl.textContent=total.toLocaleString()+' đ';
  }

  // --- Modal size/topping
  window.chonKichCoVaTopping = index=>{
    if(!kichCoList.length) return alert('Không có dữ liệu kích cỡ!');
    const item = bill[index];

    const modal = document.createElement('div');
    modal.className='fixed inset-0 flex items-center justify-center bg-black bg-opacity-40 z-50 size-modal';
    modal.innerHTML=`
      <div class="bg-white rounded-2xl p-6 w-96 shadow-lg max-h-[90vh] overflow-y-auto">
        <h2 class="text-lg font-semibold mb-4 text-center text-gray-800">Chọn tùy chọn cho <br><span class="text-blue-600">${item.ten_san_pham}</span></h2>
        <div class="mb-4">
          <h3 class="font-semibold text-gray-700 mb-2">Kích cỡ:</h3>
          <div class="flex flex-col gap-2">
            ${kichCoList.map(kc=>`
              <label class="border rounded-lg py-2 px-3 flex justify-between items-center hover:bg-blue-50">
                <input type="radio" name="kichco" value='${JSON.stringify(kc)}' ${item.kich_co===kc.ten_kich_co?'checked':''}>
                <span>${kc.ten_kich_co}</span>
                <span class="text-gray-600">+${kc.gia_them.toLocaleString()} đ</span>
              </label>`).join('')}
          </div>
        </div>
        <div class="mb-4">
          <h3 class="font-semibold text-gray-700 mb-2">Topping:</h3>
          <div class="flex flex-col gap-2">
            ${toppingList.map(t=>`
              <label class="border rounded-lg py-2 px-3 flex justify-between items-center hover:bg-blue-50">
                <input type="checkbox" value='${JSON.stringify(t)}' ${item.toppings.some(tp=>tp.topping_id===t.topping_id)?'checked':''}>
                <span>${t.ten_topping}</span>
                <span class="text-gray-600">+${t.gia_them.toLocaleString()} đ</span>
              </label>`).join('')}
          </div>
        </div>
        <div class="flex gap-2 mt-4">
          <button onclick="luuKichCoVaTopping(${index})" class="flex-1 bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700">Lưu</button>
          <button onclick="dongSizeModal()" class="flex-1 bg-gray-200 py-2 rounded-lg hover:bg-gray-300">Hủy</button>
        </div>
      </div>
    `;
    document.body.appendChild(modal);
  };

  window.luuKichCoVaTopping=index=>{
    const modal=document.querySelector('.size-modal');
    const item=bill[index];
    const kcInput=modal.querySelector('input[name="kichco"]:checked');
    if(!kcInput) return alert('Vui lòng chọn kích cỡ!');
    const kc=JSON.parse(kcInput.value);
    item.kich_co=kc.ten_kich_co;
    item.kich_co_id=kc.kich_co_id;
    item.gia_them=kc.gia_them;

    const toppingInputs=modal.querySelectorAll('input[type="checkbox"]:checked');
    const toppings=Array.from(toppingInputs).map(i=>JSON.parse(i.value));
    item.toppings=toppings;
    item.tong_topping=toppings.reduce((sum,t)=>sum+Number(t.gia_them),0);

    dongSizeModal();
    renderBill();
  };

  window.dongSizeModal=()=>document.querySelectorAll('.size-modal').forEach(m=>m.remove());
  window.changeQty=(i,d)=>{bill[i].so_luong+=d;if(bill[i].so_luong<=0) bill.splice(i,1); renderBill();};
  window.removeItem=i=>{bill.splice(i,1); renderBill();};

  // --- User
  const user=JSON.parse(localStorage.getItem('user'));
  if(!user) window.location.href='/taikhoan/dangnhap';
  else document.getElementById('tenNhanVien').textContent=`👋 Xin chào, ${user.ho_ten||user.ten_dang_nhap||'Nhân viên'}`;
  document.getElementById('logoutBtn').addEventListener('click',()=>{localStorage.removeItem('user');window.location.href='/taikhoan/dangnhap';});

  // --- Bàn
  const chooseTableBtn=document.getElementById('chooseTableBtn');
  const completeBtn=document.getElementById('completeBtn');
  const banContainerEl=document.getElementById('banContainer');
  let banList=[], hoaDonTheoBan={}, selectedBanId=null;

  fetch('http://localhost:3000/ban/laytatca')
    .then(res=>res.json())
    .then(data=>{banList=data; renderBan();});

  const storedHoaDon=localStorage.getItem('hoaDonTheoBan');
  if(storedHoaDon) hoaDonTheoBan=JSON.parse(storedHoaDon);

  chooseTableBtn.addEventListener('click',()=>{
    banContainerEl.classList.remove('hidden');
    renderBan();
  });

  function renderBan(){
    const banListEl=document.getElementById('banList');
    banListEl.innerHTML='';
    banList.forEach(ban=>{
      const div=document.createElement('div');
      div.className=`p-4 rounded-lg text-center font-semibold cursor-pointer transition
        ${ban.trang_thai==='Trống'?'bg-green-200 hover:bg-green-300':''}
        ${ban.trang_thai==='Đang sử dụng'?'bg-yellow-200 hover:bg-yellow-300':''}
        ${ban.trang_thai==='Đã thanh toán'?'bg-gray-300 cursor-not-allowed':''}`;
      div.textContent=`${ban.ten_ban} — ${ban.trang_thai}`;
      if(ban.trang_thai!=='Đã thanh toán') div.addEventListener('click',()=>selectBan(ban.ban_id));
      banListEl.appendChild(div);
    });
  }

  function closeBanModal(){ banContainerEl.classList.add('hidden'); }

  function selectBan(ban_id){
    selectedBanId=ban_id;
    banContainerEl.classList.add('hidden');
    alert(`Bạn đã chọn ${banList.find(b=>b.ban_id===ban_id).ten_ban}, nhấn Hoàn tất để lưu sản phẩm`);
    completeBtn.classList.remove('hidden');
    chooseTableBtn.classList.add('hidden');
  }

  completeBtn.addEventListener('click',()=>{
    if(!selectedBanId) return alert('Vui lòng chọn bàn!');
    if(!hoaDonTheoBan[selectedBanId]) hoaDonTheoBan[selectedBanId]=[];
    hoaDonTheoBan[selectedBanId].push(...bill.map(item=>({...item})));
    localStorage.setItem('hoaDonTheoBan', JSON.stringify(hoaDonTheoBan));
    bill.length=0; renderBill();
    completeBtn.classList.add('hidden');
    chooseTableBtn.classList.remove('hidden');
    selectedBanId=null;
    renderBan();
    alert('Đã lưu sản phẩm vào bàn');
  });
});
document.addEventListener('DOMContentLoaded', () => {
  const hoaDonTheoBan = JSON.parse(localStorage.getItem('hoaDonTheoBan')) || {};
  const viewBanContainerEl = document.getElementById('viewBanContainer');
  const viewBanListEl = document.getElementById('viewBanList');
  const viewTablesBtn = document.getElementById('viewTablesBtn');

  viewTablesBtn.addEventListener('click', () => {
    renderViewBan();
    viewBanContainerEl.classList.remove('hidden');
  });

  function renderViewBan() {
    viewBanListEl.innerHTML = '';
    const banIds = Object.keys(hoaDonTheoBan);
    if (!banIds.length) {
      viewBanListEl.innerHTML = '<p class="text-center text-gray-500">Chưa có bàn nào được chọn món</p>';
      return;
    }
    banIds.forEach(banId => {
      const banData = hoaDonTheoBan[banId];
      const div = document.createElement('div');
      div.className = 'border p-3 rounded-lg shadow-sm bg-gray-50';
      div.innerHTML = `<p class="font-semibold mb-2">Bàn ${banId}</p>`;
      if (!banData.length) {
        div.innerHTML += '<p class="text-gray-500">Chưa có sản phẩm</p>';
      } else {
        const ul = document.createElement('ul');
        banData.forEach(item => {
          const li = document.createElement('li');
          li.textContent = `${item.ten_san_pham} ${item.kich_co ? '('+item.kich_co+')' : ''} ${item.toppings.length ? '+ '+item.toppings.map(t=>t.ten_topping).join(', ') : ''} x${item.so_luong}`;
          ul.appendChild(li);
        });
        div.appendChild(ul);
      }
      viewBanListEl.appendChild(div);
    });
  }

  window.closeViewBanModal = () => {
    viewBanContainerEl.classList.add('hidden');
  };
});
  </script>

</body>
</html>
